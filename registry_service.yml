eureka:
   instance:
    hostname: localhost
   client: 
     registerWithEureka: false
     fetchRegistry: false
     serviceUrl:
      defaultZona: http://${eureka.instance.hostname}:${server.port}/eureka/
 