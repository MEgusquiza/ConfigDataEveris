server:
   port: 8096

eureka:
   instance:
    hostname: CUSTOMER_MANAGEMENT
   client: 
     registerWithEureka: false
     fetchRegistry: false
     serviceUrl:
      defaultZona: http://${eureka.instance.hostname}:${server.port}/eureka/
 